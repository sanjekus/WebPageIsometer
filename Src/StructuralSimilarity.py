class StructuralSimilarity:
	def __init__(self):
		return
	
	
	''' takes a web page as input and strips everything except the supplied tag'''
	def parsePageSeparateTag(self,webPageContent,tagName):
		
		return
	
	''' Represent the tag in the doc in a string form'''
	def getStringRepresentationOfTag(self,tagName):
		return
	
	'''take values from Constants file to calculate the distance measure of the two supplied string representatuions''' 
	def getDistanceValueFromString(self,stringRep1,stringRep2):
		return 
	
	'''loops over all the tags in the two web pages.
	for each tag that appears in the pages calculate the distance value of that tag.
	get all such distance values and calculte a final distance value '''
	def getStructuralDiffOfPages(self,webPage1,webPage2):
		return
		
		
		
		
		